spring:
  config:
    import: "optional:configserver:http://localhost:8888"

eureka:
  client:
    enabled: false
    register-with-eureka: false
    fetch-registry: false

jwt:
  secret: ${JWT_SECRET:test-secret-key-for-local-testing-only-change-me}
